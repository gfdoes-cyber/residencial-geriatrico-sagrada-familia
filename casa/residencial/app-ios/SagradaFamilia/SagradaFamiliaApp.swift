import SwiftUI

/// Ponto de entrada do aplicativo "Sagrada Família".
/// O app exibe o site institucional do Residencial dentro de um WKWebView
/// (veja `WebView.swift` e `AppConfig.swift`).
@main
struct SagradaFamiliaApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
